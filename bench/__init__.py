"""Repo-only benchmark package for zeared.

Run from the repo root::

    uv run python -m bench

Ships with the repo, never with the wheel. See ``harness.py`` for the shared
workload and result schema, and ``suite_*.py`` for the individual suites.
"""
